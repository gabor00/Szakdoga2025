{
    "name": "m1",
    "scripts": {
      "test": "jest",
      "build": "tsc -p tsconfig.lib.json" // the actual command here is arbitrary
    }
  ,
  "name": "m1", 
  "python": "python3",
  "script": {
    "serve": "flask run",
    "lint": "-m pylint",
    "test": "-m unittest discover -s ./ -p",
  }
}